apiVersion: v1
kind: Service
metadata:
  name: {{ include "kogeneesti.fullname" . }}
  labels:
    {{- include "kogeneesti.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kogeneesti.selectorLabels" . | nindent 4 }}
